Drop imported real-session JSON fixtures and their metadata sidecars in this directory.

Use `npm run fixture:import -- <path-to-awaband-scan.json> <fixture-name>` to validate and copy them here as:

- `<fixture-name>.json`
- `<fixture-name>.meta.json`
